builders:
  default: quay.io/knawd/builder:rust
  base: quay.io/knawd/builder:rust

annotations:
  module.wasm.image/variant: compat-smart
healthEndpoints:
  liveness: /health/readiness
  readiness: /health/liveness
